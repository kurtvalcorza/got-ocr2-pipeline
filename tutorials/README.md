# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/got-ocr2-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/got-ocr2-pipeline/blob/main/tutorials/got_ocr2_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-stepfun--ai%2FGOT--OCR--2.0--hf-ffcc4d?style=flat)](https://huggingface.co/stepfun-ai/GOT-OCR-2.0-hf)
[![Upstream](https://img.shields.io/badge/Upstream-Ucas--HaoranWei%2FGOT--OCR2.0-181717?style=flat&logo=github&logoColor=white)](https://github.com/Ucas-HaoranWei/GOT-OCR2.0)
[![arXiv](https://img.shields.io/badge/arXiv-2409.01704-b31b1b.svg)](https://arxiv.org/abs/2409.01704)

Notebook specification: **DIMER Notebook Specification 2.0** — the notebook is **standalone** (§4) and declares its profile and pedagogical mode (§3.4): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's three
pipeline modules (`pipeline.py`, `metrics.py`, `samples.py`), the model identity, the snapshot manifest and the
runtime pins, so the exported `.ipynb` keeps working without this repository being reachable. Do not edit the
notebook by hand; edit the package or the template and regenerate (`python tools/build_notebook.py`; `--check` is
enforced by the validator and CI).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `got_ocr2_colab.ipynb` | `E2E` | `GUIDED` | standalone (generated) | optical character recognition and bounded supervised fine-tuning with `stepfun-ai/GOT-OCR-2.0-hf` (Apache-2.0; SAM-style ViT encoder → 256 image tokens → 24-layer Qwen2 decoder, greedy decoding to `<|im_end|>`) — the safetensors snapshot digest-verified against the inline 8-file manifest before loading; the inference contract on a synthetic 1000×520 printed page with its self-rendered transcript (input manifest, unsupported-mode probe, `recognize` with sanity checks, `character_error_rate` / `word_error_rate` as `sample-sanity`); a digest-pinned real line set (800 MIT-licensed Belfort handwritten lines with crowdsourced transcripts, eight parquet row groups pinned by SHA-256 and byte total and read over HTTPS range requests at an immutable Hub revision) split by line without leakage; micro and macro CER / WER and exact match (in the carried `metrics.py`) beside the empty-string and constant-transcript baselines; bounded fine-tuning of the last four decoder layers and the final norm with the causal language-model loss on cached frozen-prefix hidden states and validation-CER epoch selection; held-out evaluation with a four-way comparison and the hypothesis lengths; six held-out lines and the printed page re-read after adaptation; safetensors adapter export with verified transcript parity | CUDA when present (CPU is not practical for the default path); float32 | 600 / 60 / 140 lines read from the Hugging Face Hub at run time (~44 MB of a 210 MB shard, MIT) | one zip or directory of line images plus a `transcripts.csv` (`file`, `text`, optional `id`; gated off by default) | ~@P:T4_TOTAL_MIN@ min on a Tesla T4 after the 1.12 GB checkpoint and the row groups download (six epochs ≈ @P:T4_ADAPT_MIN@ min) | **Candidate** — the supported-runtime run of the exact candidate blob is not yet recorded in `../docs/release-verification.md` |

## What changed from the inference-only tutorial

The previous notebook (`TASK-INFERENCE`) recognised the synthetic printed page and reported `sample-sanity` error rates against its rendered transcript. This notebook keeps that page and its inference contract as Section 5 and adds the adaptation contract around it: a real labelled line set fetched with per-row-group pins, a corpus-level protocol in the carried `metrics.py` (uncapped micro and macro CER / WER, exact match, hypothesis length) with two non-adapted baselines, a bounded fine-tuning with the model's own objective on cached frozen-prefix states, line panels, the page re-read after adaptation, and an artifact that reloads. The per-image `evaluation_report` helper is unchanged; `pipe.evaluate` is the corpus-level stage (`measured` / `measured-small-sample`). The pipeline now loads in float32 on every device (the inference-only notebook used bfloat16 on CUDA), batches generation, and admits text lines wider than 4,096 px.

## Conformance notes

- **Standalone carrier (ST1–ST8):** the primary path performs no clone, repository install, or repository import. Section 2 embeds the repository's three modules verbatim in topological dependency order (each tagged `metadata.dimer.embedded_module`; the only rewrites make `DEFAULT_WEIGHTS_DIR` working-directory-relative and remove internal relative package imports), Section 3 carries the 8-file snapshot manifest inline and asserts it against the module's constants before fetching, and `NOTEBOOK_SOURCE` records the repository, revision and module digests in the exports.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried modules, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Model acquisition (MOD1–MOD9, MOD12):** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries a fresh runtime lacks, at the pinned revision (never `main`); `verify_snapshot` re-hashes every entry and asserts the pinned `model.safetensors` digest and byte count before the processor or the model is constructed; `GotOcr2Pipeline.from_pretrained(weights_dir=WEIGHTS_DIR)` loads the verified files (`local_files_only=True`, `trust_remote_code=False`) in float32 and picks CUDA when it is visible. No cell outside the carried modules calls `huggingface_hub`, `transformers`, `safetensors`, `pyarrow`, `urllib`, `torch.optim` or the model's processor directly.
- **Data (DAT1–DAT9, ST6):** the default sample is the Belfort-line dataset (`Teklia/Belfort-line` at the immutable parquet-conversion revision `c4a74bbd39f2df314752e7e6026649a39d365cbb`, MIT): the first eight row groups of `default/test/0000.parquet` read with HTTPS range requests in the carried `samples.py` (the shard's declared size and row count checked first, each row group refused unless its SHA-256 and byte total match `ROW_GROUP_PINS`; about 44 MB), and `read_corpus` makes each row a record with its 128-px line image and its transcript (whitespace runs collapsed); `build_sample_dataset(seed=SPLIT_SEED)` draws 600 / 60 / 140 and `check_split_disjoint` asserts no image (by decoded-pixel digest) is shared. The template adds a second *External access* bullet naming the host, the revision, the byte count and the licence.
- **Stages (§4.1):** `fetch_corpus` + `read_corpus` + `build_sample_dataset` / `load_byod_dataset` + `split_dataset` (sample acquisition) → `validate_dataset` per split + `check_split_disjoint` + refusal probes (validation, with three digests) → `validate_inputs` + `recognize` with sanity checks + `evaluation_report` on the synthetic page (inference contract) → `empty_baseline` + `constant_baseline` + `pipe.evaluate` on the frozen model with four hypotheses printed (baselines) → `pipe.adapt` (bounded fine-tuning) → `pipe.evaluate` on validation and test with the comparison (evaluation) → the line panels + `recognize` + `evaluation_report` on the page again (inference) → `pipe.save_artifact` → `GotOcr2Pipeline.from_artifact` with the parity assertion (export and reload).
- **Score semantics (UNC1–UNC4):** generated text carries no score, no probability and no correctness signal, and the notebook says so before the first recognition; the corpus rates are uncapped Levenshtein rates over one crowdsourced transcription at stated normalisations, with the hypothesis length beside them so a rate above 1.0 is read as over-generation; no acceptance threshold ships. The page re-read after adaptation carries a `sample-sanity` verdict that names the missing dispersion.
- **Baselines and evidence (EVAL6/EVAL10/EVAL11):** the empty string, the constant transcript and the frozen model are scored on the same 140 lines as the adapted model, on the same rates; every metric is labelled as one seeded split of one sample with no dispersion estimate, and the notebook says explicitly that the frozen model sits below the empty baseline (it invents text) and that the gain is measured on one French council's cursive only.
- **Split integrity (SPL1–SPL8):** the split is seeded and image-disjoint; validation (60 lines) selects the epoch on CER and the test split is used for nothing but the final evaluation; the notebook states that lines cut from the same page share a hand and must be split by page, writer or volume.
- **Ceilings (VAL6):** `MIN_IMAGE_SIDE` (16), `MAX_IMAGE_SIDE` (16384), `MAX_IMAGE_PIXELS` (4096²), `MAX_NEW_TOKENS` (4096), `DEFAULT_MAX_NEW_TOKENS` (1024), `DEFAULT_LINE_MAX_NEW_TOKENS` (128), the two `MODES`, `DECODING`, `STOP_STRING`, the record bounds (8..5,000), `MAX_TEXT_CHARS` (512) and the id pattern are stated in the Prerequisites and printed before the model runs; four dataset refusals and one input refusal are demonstrated.
- **BYOD (DAT10–DAT19):** `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog; when enabled, one zip is read member by member (names flattened, no `extractall`) or a directory walked, each row of `transcripts.csv` becomes a record, and the records flow through the same validation, image-disjoint split, baselines, fine-tuning, evaluation, export and reload cells. The expected layout and the privacy guidance are stated in the Prerequisites and in Section 4, and the training split's summary is written to `outputs/got_ocr2_train.csv`.
- Licence: the weights are Apache-2.0 and the notebook, README, card and `docs/WEIGHTS.md` say so; Belfort-line is MIT and is not redistributed — the export records the corpus name, repository, revision, file, licence, language and row groups under `corpus`.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the clean-runtime execution requirement; a release review must confirm that a recorded clean run in `../docs/release-verification.md` matches the notebook revision under review before the status is promoted to `Release-grade`.

## Outputs

The default notebook path writes (under `outputs/`):

- `got_ocr2_train.csv` — the training split's summary table (id, file name, image size, character and word counts, transcript, provenance)
- `got_ocr2_example_line.png` — the first training line
- `got_ocr2_input_manifest.json` — the inference-contract manifest (schema, per-input observations, one recorded rejection finding)
- `got_ocr2_page_frozen.txt`, `got_ocr2_page_adapted.txt` — the synthetic page's transcript before and after adaptation
- `got_ocr2_evaluation_report.json` — baselines, frozen and adapted metrics on validation and test, the per-line rows with both hypotheses, the comparison, the adaptation history
- `got_ocr2_examples/` — six held-out lines as panels (the line image with the reference, the frozen transcript and the adapted transcript)
- `got_ocr2_adapter/` — `adapter.safetensors` + `manifest.json`
- `got_ocr2_result.json` — `NOTEBOOK_SOURCE`, model identity, snapshot, corpus, inference-contract reports, comparison, artifact digest, reload parity, runtime

The earlier record for the superseded `TASK-INFERENCE` carrier is kept in `../docs/release-verification.md`.

## AI Assistance Disclosure

This repository’s code and accompanying documentation were developed with generative AI assistance for code development and technical writing under maintainer direction. The maintainer remains responsible for reviewing the implementation, validating results, and making release decisions. AI assistance does not constitute independent verification, provider endorsement, or release approval.
