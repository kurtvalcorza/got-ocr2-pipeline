# Release status

Current status: **Candidate — tutorial source complete, model-backed pre-flight executed, awaiting supported-runtime execution**. The pipeline package (inference plus the adaptation contract: the Belfort line sample, corpus CER/WER, bounded fine-tuning of the last four decoder layers, the verified adapter), offline unit tests (including the adaptation, role-helper, import-boundary and notebook-parity suites), the model-backed suite, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/got_ocr2_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR4); the package-API recipe ran on a Kaggle Tesla T4 as pre-flight and is recorded in `docs/release-verification.md`. None of that is supported-runtime notebook evidence; the repository is not release-grade until a Colab/Kaggle execution of the exact candidate notebook blob is recorded there.
